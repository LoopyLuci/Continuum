#[deprecated(
    note = "continuum-ai is not wired into the current runtime and is excluded from default test/doc runs until its Windows rustdoc toolchain issue is resolved"
)]
pub mod ai_media;
#[deprecated(
    note = "continuum-ai is not wired into the current runtime and is excluded from default test/doc runs until its Windows rustdoc toolchain issue is resolved"
)]
pub mod anomaly_detector;
#[deprecated(
    note = "continuum-ai is not wired into the current runtime and is excluded from default test/doc runs until its Windows rustdoc toolchain issue is resolved"
)]
pub mod intent_agent;
#[deprecated(
    note = "continuum-ai is not wired into the current runtime and is excluded from default test/doc runs until its Windows rustdoc toolchain issue is resolved"
)]
pub mod network_predictor;
#[deprecated(
    note = "continuum-ai is not wired into the current runtime and is excluded from default test/doc runs until its Windows rustdoc toolchain issue is resolved"
)]
pub mod onnx_engine;
#[deprecated(
    note = "continuum-ai is not wired into the current runtime and is excluded from default test/doc runs until its Windows rustdoc toolchain issue is resolved"
)]
pub mod segmentation;
#[deprecated(
    note = "continuum-ai is not wired into the current runtime and is excluded from default test/doc runs until its Windows rustdoc toolchain issue is resolved"
)]
pub mod super_resolution;

#[deprecated(
    note = "continuum-ai is not wired into the current runtime and is excluded from default test/doc runs until its Windows rustdoc toolchain issue is resolved"
)]
pub use intent_agent::IntentAgent;
#[deprecated(
    note = "continuum-ai is not wired into the current runtime and is excluded from default test/doc runs until its Windows rustdoc toolchain issue is resolved"
)]
pub use network_predictor::NetworkForecaster;
#[deprecated(
    note = "continuum-ai is not wired into the current runtime and is excluded from default test/doc runs until its Windows rustdoc toolchain issue is resolved"
)]
pub use onnx_engine::OnnxInferenceEngine;
